import 'package:flutter/material.dart';

class ThemeConfig {
   // Primary colors
  static const Color primary = Color(0xFFF9803B);
  static const Color primarySoft = Color(0xFFFFE5D5);

  // Accent colors
  static const Color accent = Color(0xFF00A8E8);
  static const Color accentSoft = Color(0xFFD6F0FF);

  // Backgrounds
  static const Color lightBackground = Color(0xFFFFFFFF);
  static const Color darkBackground = Color(0xFF000000);

  // Surface
  static const Color lightSurface = Color.fromARGB(255, 255, 255, 255);
  static const Color darkSurface = Color(0xFF1C1C1C);

  // Text
  static const Color lightText = Color(0xFF000000);
  static const Color lightSecondaryText = Color(0xFF6B6B6B);
  static const Color darkText = Color(0xFFFFFFFF);
  static const Color darkSecondaryText = Color(0xFFB0B0B0);

  // Other
  static const Color purpleGlow = Color(0xFFE0BBFF);
  static const Color error = Color(0xFFFF4C4C);

  /// fonts family
  static const pangramRegular = 'Pangram Regular';
  static const pangramMedium = 'Pangram Medium';
  static const pangramBold = 'Pangram Bold';
  static const pangramExtraBold = 'Pangram Extra Bold';
  static const pangramLight = 'Pangram Light';
  static const lufgaRegular = 'Lufga Regular ';
}
